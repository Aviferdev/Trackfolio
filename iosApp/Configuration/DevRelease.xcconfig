// DevRelease: entorno dev + build release (optimizado, para TestFlight interno)
// El include de Pods DEBE ir antes que el del proyecto (ver DevDebug.xcconfig).
#include "Pods/Target Support Files/Pods-iosApp/Pods-iosApp.devrelease.xcconfig"
#include "Dev.xcconfig"

ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon-Debug
